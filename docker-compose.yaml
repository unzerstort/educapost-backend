services:
  api:
    build: .
    container_name: educapost-api
    ports:
      - "3000:3000"
    volumes:
      - educapost-data:/usr/src/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped

volumes:
  educapost-data: